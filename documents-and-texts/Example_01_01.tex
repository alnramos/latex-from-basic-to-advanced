% LaTeX
% Allan de Alencar Ramos
% Document's General Structure

% Basic structure

% The \documentclass[]{} and \usepackage[]{} commands make up what we call the Preamble.
% The Preamble contains information about the document's characteristics.

\documentclass[]{book}

% Possible parameters inside the brackets
% [  
%    font size: 10pt | 11pt | 12pt,
%    twoside: fills both sides of the page,
%    oneside: fills only one side of the page,
%    twocolumn: the text is created in two columns,
%    onecolumn: the text is created in a single column,
%    landscape: the document will be in landscape format,
%    leqno: formula numbering on the left,
%    fleqn: the formula is aligned to the left margin instead of being centered,
%    openright: chapters only start on odd-numbered pages,
%    openany: chapters start on odd or even pages
%    paper size: a4paper | letterpaper
% ]

% Possible parameters inside the braces
% {  
%    article: for academic articles,
%    report: for reports,
%    book: for books, theses, and similar documents,
%    letters: for letters
% }

% A package allows you to include new features in the document.
% To find new packages: https://ctan.org/pkg
\usepackage[]{}

\begin{document}

% Your code goes here

\end{document}
